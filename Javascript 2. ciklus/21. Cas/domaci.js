const myfunction = (arr) => {
	let maximum = Number.NEGATIVE_INFINITY;
	for (number of arr) {
		if (number > maximum) {
			maximum = number;
		}
	}
	return maximum;
};
console.log(myfunction([12, -32, 32, 13, 52, 32, 17]));
